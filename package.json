{
  "name": "sluggr",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:libs": "npm run build -w @locket/vortex-engine && npm run build -w @locket/ai-brain && npm run build -w @locket/matching",
    "dev": "npm run build:libs && npm run dev -w web",
    "build": "npm run build:libs && npm run build -w web",
    "lint": "npm run lint -w web"
  }
}
